# Используем официальный образ Node.js с версией 22.14.0 (на базе Alpine для легковесности)
FROM node:22.14.0-alpine

# Задаём рабочую директорию внутри контейнера
WORKDIR /app

# Копируем файлы зависимостей и устанавливаем зависимости
COPY package*.json ./
RUN npm install

# Копируем исходный код приложения
COPY . /app

# Экспонируем порт, который используется приложением (8082)
EXPOSE 8082

# Запускаем приложение в режиме разработки
CMD ["npm", "run", "dev"]
